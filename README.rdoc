== Media Center

This is a project that goes over the paradigm `frustration over configuration`. It servers all your video files on the app, grouped by directory.

Steps are necessary to get the application up and running:

Things you may want to cover:

* Ruby version - 2.0

* System dependencies - Fucking sprockets removed

* Configuration

* Database creation - There's no need for a database
